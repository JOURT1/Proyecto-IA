# CPU installation profile, to be used after installing PyTorch CPU manually.
#
# Recommended installation:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-cpu.txt
#
# Keeping torch out of this file prevents pip from resolving the default CUDA
# wheels on machines where the CPU wheel index is not selected globally.

ultralytics>=8.0.176
opencv-python>=4.8.1
numpy>=1.24.3
pandas>=2.0.3
pyyaml>=6.0
streamlit>=1.28.1
Pillow>=10.0.0
matplotlib>=3.7.2
scikit-learn>=1.3.0
scipy>=1.11.2
tqdm>=4.66.1
requests>=2.31.0
